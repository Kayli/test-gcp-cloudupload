{
  "name": "gcp-docstore-prototype",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon --watch src --watch public --exec node src/index.js",
    "test": "playwright test --workers=1",
    "test:e2e": "playwright test test/ui.spec.js --workers=1",
    "test:all": "playwright test --workers=1",
    "lint": "echo 'no lint configured'"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "google-auth-library": "^8.7.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.36.0",
    "chrome-remote-interface": "^0.34.0",
    "nodemon": "^2.0.22",
    "puppeteer-core": "^24.39.1"
  }
}
